# See https://www.robotstxt.org/robotstxt.html for documentation on how to use the robots.txt file
#
# The library is infinite; crawl a page, not the whole thing. Book pages
# are generated on demand from their address, so nothing here is static.
User-agent: *
Disallow: /books/random
